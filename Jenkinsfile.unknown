pipeline{
    agent any
    stages {
        stage('hotfix-changes...') {
            steps {
                sh 'echo "hotfix latest changes" '
            }
        }
        stage('test') {
            steps {
                sh 'echo "testing applicaiton....." '
            }
        }
        stage('deploy application') {
            steps {
                sh 'echo "deploy application........" '
            }
        }
    }
}
