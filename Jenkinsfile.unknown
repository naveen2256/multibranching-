pipeline{
    agent any
    stages {
        stage('sprint1-changes...') {
            steps {
                sh 'echo "sprint1 latest changes" '
            }
        }
        stage('test') {
            steps {
                sh 'echo "testing applicaiton....." '
            }
        }
        stage('deploy application') {
            steps {
                sh 'echo "deploy application........" '
            }
        }
    }
}
